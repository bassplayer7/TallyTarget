<section class="wrapper style3 container 80%">
    <h2>{{ page.header.title }}</h2>
    <div class="row">
        <div class="7u 12u(narrower)">
            {{ content }}
        </div>
        <div class="5u 12u(narrower)">
            <img src="{{ page.media[page.header.image].url }}" alt="{{ page.header.image_alt }}" class="column-image align-center" />
        </div>
    </div>
</section>